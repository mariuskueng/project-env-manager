# Dependencis explained

- chrome-type
  - add types for chrome so when we hover for example the chrome object in popup.js we get proper type information. This needs to be supported by your editor. VS Code does support it.
- prettier
  - to prettiefy and unify the code style
