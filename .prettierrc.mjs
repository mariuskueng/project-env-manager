const config = {
  semi: false, // we need this to keep the first semicolon in js file which ensures they are run even if something before them crashed. https://stackoverflow.com/a/7145534
}

export default config
